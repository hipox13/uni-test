# Database (PostgreSQL)
DATABASE_URL="postgresql://user:password@localhost:5432/unicef-redev-db"

# Server
PORT=3000
NODE_ENV=development

# JWT (for auth)
JWT_SECRET=your-secret-key
JWT_EXPIRES_IN=7d
